
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: root
  jpa:
    database: mysql
    generate-ddl: true
    hibernate:
      ddl-auto: none
    show-sql: true
  redis:
    host: localhost
    port: 6379


server:
  port: 8181

external:
  late-fee: 1000
  renew-days: 7
